import 'package:flutter/material.dart';

class AppColors {
  static const primaryColor = Color(0xFF006837);
  static const secondaryColor = Color(0xFF006837);
  static const headingColor = Color(0xFF404040);

  static const splashFilterColor = Color(0x99011300);

  static const cardTitleColor = Colors.black;
  static const errorColor = Colors.redAccent;
  static const scaffoldColor = Colors.white;
  static const borderColor = Colors.grey;
  static const textFiledColor = Color(0x3FD9D9D9);

  static const white = Colors.white;
  static const black = Colors.black;
  static const grey = Colors.grey;
}
